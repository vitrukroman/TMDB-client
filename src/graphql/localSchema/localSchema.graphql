
extend type Query {
    language: Language!
}

extend type Mutation {
    updateLanguage(language: String!): Boolean!
}

type MovieLang {
    value: Language!
}

extend type Movie {
    lang: MovieLang!
}